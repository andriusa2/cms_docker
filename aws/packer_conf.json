{
  "variables": {
    "aws_access_key": "{{env `AWS_ACCESS_KEY_ID`}}",
    "aws_secrete_key": "{{env `AWS_SECRET_ACCESS_KEY`}}",
    "debfile": "{{env `DEBFILE`}}",
    "region": "eu-west-1"
  },
  "builders": [
    {
      "type": "amazon-chroot",
      "nvme_device_path": "/dev/nvme1n1p",
      "device_path": "/dev/sdf",
      "access_key": "{{user `aws_access_key`}}",
      "ami_name": "cms-release-{{isotime | clean_resource_name}}",
      "region": "{{user `region`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "source_ami_filter": {
        "filters": {
          "virtualization-type": "hvm",
          "name": "*debian-11-*",
          "root-device-type": "ebs",
	  "architecture": "x86_64"
        },
        "owners": ["136693071363"],
        "most_recent": true
      }
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "{{user `debfile`}}",
      "destination": "/tmp/cms_aws-1_amd64.deb"
    },
	  {
      "type": "file",
      "source": "send_logs/",
      "destination": "/tmp/"
    },
    {
      "type": "shell",
      "inline": [
        "apt-get update",
        "apt install -y /tmp/cms_aws-1_amd64.deb",
        "install -g root -o root -m 755 /tmp/cmsSendLogs /usr/local/bin/cmsSendLogs",
        "install -g root -o root -m 644 /tmp/cms-send-logs.service /etc/systemd/system/cms-send-logs.service",
        "systemctl daemon-reload",
        "systemctl enable cms-send-logs"
      ]
    }
  ]
}
